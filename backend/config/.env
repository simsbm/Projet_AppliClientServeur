# Backend Environment Variables
# Copy this file to .env and update with your actual values

# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=00000000
DB_NAME=school_management_system

# JWT Configuration
# IMPORTANT: Change this to a random, secure string in production
JWT_SECRET='960s2xrghb0vb5f6hjk6yt5b2c6f5gh6e3d5g4u4ldf5zr6sup45qxc9mp0P'

# CORS Configuration (optional)
# ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# Notes:
# 1. Never commit the .env file to version control
# 2. Always use strong, unique values for JWT_SECRET in production
# 3. Update DB_PASSWORD with your actual MySQL root password
# 4. For production, set NODE_ENV=production